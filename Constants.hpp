
// Размеры окна
const int WINDOW_WIDTH = 800;
const int WINDOW_HEIGHT = 600;

// Настройки уровня (если вам понадобятся конкретные параметры для вашего уровня)
const int TILE_SIZE = 32;  // Размер одного тайла в пикселях
const int LEVEL_WIDTH = WINDOW_WIDTH / TILE_SIZE;   // Количество тайлов по ширине
const int LEVEL_HEIGHT = WINDOW_HEIGHT / TILE_SIZE; // Количество тайлов по высоте

// Настройки игрока
const float PLAYER_SPEED = 200.0f; // скорость игрока в пикселях в секунду
const int PLAYER_WIDTH = 32;  // ширина спрайта игрока в пикселях
const int PLAYER_HEIGHT = 32; // высота спрайта игрока в пикселях

// Прочие настройки (добавьте любые другие константы, которые вам понадобятся)
